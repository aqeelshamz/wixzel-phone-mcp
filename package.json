{
  "name": "wixzel-phone-sdks",
  "version": "0.0.0",
  "private": true,
  "description": "Public source for the published Wixzel Phone packages. Mirrored from the monorepo that also holds the API.",
  "license": "MIT",
  "workspaces": [
    "packages/sdk-ts",
    "apps/mcp"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/aqeelshamz/wixzel-phone-sdks.git"
  },
  "homepage": "https://docs.phone.wixzel.com/sdks"
}
