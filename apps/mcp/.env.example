# Your Wixzel Phone API key (Dashboard → API keys → Create key).
WIXZEL_API_KEY=wv_live_...
# Override only for local development against a self-hosted API.
# WIXZEL_API_BASE_URL=https://api.phone.wixzel.com
# Pin API behaviour to a date (optional).
# WIXZEL_API_VERSION=2026-09-01
