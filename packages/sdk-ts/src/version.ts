/** Kept in step with package.json by a test; bump both together. */
export const VERSION = '0.1.0';
export const USER_AGENT = `wixzel-phone/${VERSION}`;
export const CLIENT_HEADER = `wixzel-phone-ts/${VERSION}`;
